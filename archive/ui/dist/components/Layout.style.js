import { style } from 'typestyle';
export const box = style({});
export const row = style({
    display: 'flex',
    flexDirection: 'row',
});
export const wrappingRow = style({
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
});
export const column = style({
    display: 'flex',
    flexDirection: 'column',
});
